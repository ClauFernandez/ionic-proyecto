import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dapd-proyecto-final',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
